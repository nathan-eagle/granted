#!/usr/bin/env node

console.log('\n[ChatKit] Devtool placeholder')
console.log('---------------------------------------------')
console.log('Embed ChatKit devtools by launching the hosted playground at https://chatkit.studio/playground while the Next dev server is running.')
console.log('Once the ChatKit CLI is released, replace this script with the official `chatkit devtool` invocation.')
console.log('For now, keep environment variables (CHATKIT_WORKFLOW_ID, OPENAI_API_KEY) in sync and document findings in docs/agentkit-notes.md.\n')
